<tr>
  <th scope="row">{{ include.item.date | date: '%b %Y' }}</th>
  <td>
    {% if include.item.inline %}
      {{ include.item.content | remove: '<p>' | remove: '</p>' | emojify }}
    {% else %}
      <a class="news-title" href="{{ include.item.url | relative_url }}">{{ include.item.title }}</a>
    {% endif %}
  </td>
</tr>
